CREATE TABLE FLOWERS(ID BIGINT PRIMARY KEY AUTO_INCREMENT, 
                  NAME VARCHAR(30), POPULATION BIGINT);

CREATE TABLE ORDERS(ID BIGINT PRIMARY KEY AUTO_INCREMENT, 
                  SHOPNAME VARCHAR(30), TOTAL FLOAT(8), OUTSTANDING FLOAT(8));